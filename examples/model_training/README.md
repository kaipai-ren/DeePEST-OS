# A Guide to model training

## Training steps

- First, use `split_dataset.py` to split demo.xyz into training and test sets. 

- Then run `python run_train.py --config=config.yaml` to train the model.

## About the dataset

Here `demo_train.xyz` and `demo_test.xyz` are small training datasets used to test the model training process. 